<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ni.gob.minsa.comunitaria.familiar"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
    
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".preferences.PreferencesActivity" />
        <activity android:name=".activities.ListDepartamentosActivity" />
        <activity android:name=".activities.ListMunicipiosActivity" />
        <activity android:name=".activities.ListSectoresActivity" />
        <activity android:name=".activities.ListComunidadesActivity" />
        <activity android:name=".activities.DownloadActivity" />
        
    </application>

</manifest>
